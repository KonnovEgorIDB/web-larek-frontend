# Web-Larek

**Web-Larek** — это интернет-магазин товаров для веб-разработчиков. Пользователи могут просматривать каталог товаров, добавлять их в корзину и оформлять заказы с пошаговой формой оплаты и контактной информацией.  

---

## 🛠 Технологии

- **TypeScript** — строгая типизация моделей и интерфейсов
- **HTML5 / CSS3 / SCSS** — вёрстка и стили
- **JavaScript (ES6+)** — логика приложения и взаимодействие с API
- **Fetch API** — работа с данными с сервера
- **Модульная архитектура** — разделение моделей и представлений (MVC-подобно)

---

## 📦 Функционал

### Главная страница

- Отображение каталога товаров с карточками
- Нажатие на карточку открывает модальное окно с детальной информацией о товаре
- Поддержка клика по кнопке "В корзину"
- Цены отображаются корректно; если цена отсутствует — товар считается бесценным, кнопка покупки деактивирована

### Корзина

- Иконка корзины в шапке открывает модальное окно с товарами
- Подсчёт количества и общей суммы товаров
- Возможность удалить товар из корзины
- Кнопка оформления заказа активна только при наличии товаров

### Оформление заказа

**Шаг 1: Способ оплаты и адрес доставки**

- Выбор между "онлайн" и "наличными"
- Ввод адреса доставки
- Проверка валидности формы: кнопка "Далее" активна только после выбора оплаты и заполнения адреса
- Показ ошибок при некорректных данных

**Шаг 2: Контакты покупателя**

- Ввод email и телефона
- Проверка валидности полей: кнопка "Оплатить" активна только при заполнении всех полей
- Отправка заказа на сервер через API
- Показ модального окна с подтверждением успешного оформления

**Шаг 3: Подтверждение**

- Показ окна с информацией о списанной сумме
- Кнопка закрытия окна и очистка корзины и формы


---

## 🔧 Модели данных

- `Item` — модель товара
- `CatalogModel` — хранение и управление товарами
- `BasketModel` — корзина с товарами и подсчёт суммы
- `OrderModel` — хранение информации о заказе, контактах и оплате

---

## 🖥 Представления (Views)

- `CatalogView` — отображение каталога на главной странице
- `CardView` — модальное окно товара с кнопкой "В корзину"
- `BasketView` — модальное окно корзины
- `BasketItemView` — отдельная карточка товара в корзине
- `OrderFirstView` — форма выбора оплаты и адреса
- `OrderSecondView` — форма ввода контактов
- `OrderSuccessView` — окно подтверждения успешного заказа

---

## ⚡ Особенности реализации

- Используется делегирование событий и обработка кликов на уровне модального окна
- Применён строгий контроль состояния корзины и товаров (товар нельзя добавить дважды)
- Разделение моделей и представлений: можно заменить любое представление без изменения моделей
- Валидация форм осуществляется на клиентской стороне
- Поддержка работы с API для получения товаров и отправки заказов

---

## 🚀 Как запустить

Клонировать репозиторий:



Установить зависимости:
```bash
npm install
```
Собрать проект:
```bash
npm run build
```
Запустить локальный сервер:
```bash
npm start
```
Открыть в браузере:
```bash
http://localhost:3000
```